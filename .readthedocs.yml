# Required
version: 2

# Build Environment
build:
  os: "ubuntu-20.04"
  tools:
    python: "3.7"

# Python configuration
python:
  version: 3.7
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
